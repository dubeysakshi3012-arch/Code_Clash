# FastAPI and server
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# Database
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary>=2.9.9,<3.0.0

# Authentication and security
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt>=4.0.0
python-multipart==0.0.6

# Validation and settings
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0
email-validator>=2.1.0

# Redis (optional, for caching)
redis==5.0.1

# Celery (task queue)
celery>=5.3.0
kombu>=5.3.0

# Docker SDK (for code execution)
docker==6.1.3

# HTTP client (for OpenAI API)
httpx==0.25.1

# Google Gemini (match question generation)
google-generativeai>=0.3.0

# Groq (match question generation, free tier)
groq>=0.4.0

# Requests (pinned for docker-py compatibility on Windows)
# docker-py 6.1.3 requires requests < 2.32.0 due to custom URL scheme support
requests==2.31.0

# Rate limiting
slowapi==0.1.9

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
ruff==0.1.6
