# CodeClash Docker - copy to .env in project root
# Required for docker-compose up

JWT_SECRET_KEY=change-me-to-a-secure-random-string
SOCKET_SERVER_SECRET=change-me-same-as-above-or-different-secret

# Optional - AI question generation (leave empty to use DB fallback)
GEMINI_API_KEY=
GROQ_API_KEY=
